<h1>Beers Index</h1>

<% @beers.each do |beer| %>
  <h2><%= beer.brewery.name %> <%= beer.name %></h2>
  <div>
    <p><%= beer.style %> - <%= beer.ABV %></p>
  </div>
  <%= link_to "Rate Beer", new_beer_rating_path(beer.id)%>
<% end %>